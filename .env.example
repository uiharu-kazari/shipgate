# Gemini (Google Cloud AI — required tech). Create a key at https://aistudio.google.com
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Elasticsearch (sponsor tech) — evidence store
ELASTICSEARCH_URL=
ELASTICSEARCH_API_KEY=
ELASTICSEARCH_INDEX=shipgate-evidence

# Optional: post verdicts as PR comments directly from the agent
GITHUB_TOKEN=
