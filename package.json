{
  "name": "shipgate",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "agent",
    "demo-app"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:agent": "npm run dev -w agent",
    "dev:demo": "npm run dev -w demo-app",
    "build": "npm run build -ws"
  }
}
